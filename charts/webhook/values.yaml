backend:
  image:
    repository: akiratomori/yas-webhook
    tag: latest

  nameOverride: webhook
  fullnameOverride: webhook
  databaseName: webhook
  replicaCount: 1
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
