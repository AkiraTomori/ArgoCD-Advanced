write:
  replicas: 1
  persistence:
    volumeClaimsEnabled: false
read:
  replicas: 1
backend:
  replicas: 1
  persistence:
    volumeClaimsEnabled: false
loki:
  commonConfig:
    replication_factor: 1
  auth_enabled: false
  useTestSchema: true
  storage:
    bucketNames:
      chunks: chunks
      ruler: ruler
      admin: admin
test:
  enabled: false
storage:
  type: 'filesystem'
minio:
  enabled: false

monitoring:
  selfMonitoring:
    enabled: false
    grafanaAgent:
      installOperator: false