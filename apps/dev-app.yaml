apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: yas-dev-environment
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/AkiraTomori/ArgoCD-Advanced.git"
    targetRevision: HEAD
    path: helm-chart
    helm:
      valueFiles:
        - ../environments/values-dev.yaml
  destination:
    server: "https://kubernetes.default.svc"
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
